services:
  postgres:
    restart: unless-stopped

  api:
    restart: unless-stopped
    environment:
      LOG_LEVEL: INFO # Overrides local DEBUG
      ALLOWED_ORIGINS: "https://your-production-domain.com" # Lock down CORS

  web:
    restart: unless-stopped

  mobile:
    restart: unless-stopped