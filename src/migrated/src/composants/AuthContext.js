// Compatibility shim – old pages import AuthContext from '../composants/AuthContext'.
// This re-export keeps those imports working without touching the old JS files.
export { AuthContext } from '../context/auth-context';
